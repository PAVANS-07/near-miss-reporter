*Write Flyway V1 migration — core table with all required
columns, correct types, indexes on key fields
*Setup React with Vite — install Axios and Tailwind, set API
base URL, create pages/ components/ services/ folders
*Build list page — table, Axios GET on mount, loading state,
empty state. Write aFlyway V2 migration for audit_log
*Build REST — PUT /{id}, DELETE (soft), GET /search?q=.
Build create/edit form with validation in React
*React login page — AuthContext, ProtectedRoute, connect
list page to real GET /all API with pagination
*Dashboard — 4 KPI cards from /stats, Recharts chart. Detail
page with score badge, Edit/Delete buttons
*Search/filter bar — debounced search, status dropdown,
date range. Audit logging via Spring AOP on CUD methods
*MockMvc tests — all endpoints, correct status codes. AI
panel in UI — loading spinner, formatted response card
*Swagger/OpenAPI on all endpoints. Add GET /export CSV.
File upload POST /upload with type and size validation
*Data seeder — 15 realistic demo records on startup.
Analytics page with Recharts charts and period selector
*Responsive design — 375px/768px/1280px all working.
Performance — EXPLAIN ANALYZE, add indexes, fix N+1
Not
*Team code review — no secrets, no TODO comments,
standards enforced. Final merge, tag release/v1.0
*Record 90-sec demo video — all key features with voice
narration, save as demo_video.mp4
*Fix all rehearsal issues. Prepare 3 slides — problem,
architecture, demo flow (1 min total for slides)
*Fresh machine test — clone, create .env, docker-compose
up --build, full stack healthy.
*Prepare 3 demo scenarios — exact steps, expected output,
talking points. Confirm all work on live system
*Final hotfix window — P1/P2 only, re-test after each fix Not
*Submit final SECURITY.md — professional, complete,
accurate. Print 1 copy for Demo Day
*DEMO — Security: 401 without token, 400 on injection,
reference SECURITY.md, state findings fixed